{
  "name": "hero-travel-monorepo",
  "version": "0.0.1-beta",
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "remove-modules": "lerna exec -- rm -rf ./node_modules"
  },
  "dependencies": {
    "moment": "^2.18.1",
    "numeral": "^2.0.6"
  }
}
