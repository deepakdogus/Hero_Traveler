{
  "name": "hero-travel-monorepo",
  "version": "0.0.1-beta",
  "devDependencies": {
    "lerna": "^2.0.0-rc.5"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "remove-modules": "lerna exec -- rm -rf ./node_modules",
    "build": "docker-compose build",
    "deploy": "docker stack deploy --with-registry-auth --compose-file=docker-compose.yaml api",
    "start-dev": "scripts/start-dev"
  }
}
