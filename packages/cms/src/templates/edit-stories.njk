{% extends 'index.njk' %}

{% block main %}
  <form enctype="application/json" action="/stories/edit?id={{data._id if data._id else 'new'}}" method="post" class="sm-col-4 m3">
    <label>Title</label>
    <input name="title" type="text" value="{{data.title}}" class="block col-12 mb1 field">
    <label>Sponsored By</label>
    <input name="sponsoredBy" type="text" value="{{data.sponsoredBy}}" class="block col-12 mb1 field">
    <label class="block col-12 mb2">
      <input name="feature" type="checkbox" checked>
      Feature
    </label>
    <label class="block col-12 mb2">
      <input name="featureAll" type="checkbox" checked>
      Feature All
    </label>
    <label class="block col-12 mb2">
      {% if data.flagged %} <input name="flagged" type="checkbox" checked>
      {% else %} <input name="flagged" type="checkbox">
      {% endif %}
      Flagged
    </label>
    <button type="submit" class="btn btn-primary">Change</button>
    <button type="reset" class="btn btn-primary black bg-gray">Cancel</button>
  </form>
  <button class="btn bg-red ml2"><a class="white" href="/stories/delete?id={{data._id}}">Delete Entry</a></button>
  <div class="m2">{{ data }}</div>
{% endblock %}
